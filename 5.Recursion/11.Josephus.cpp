#include <bits/stdc++.h>

using namespace std;

// In this problem we are given n number of
// person , each time we have to kill the kth person

void josephus(int arr[], int n, int k, int start = 0)
{
    if (start == k)
    {
    }
}
{
}

int main()
{
    int n = 7, k = 3;
    vector<int> m(n);
    for (int i = 0; i < n; i++)
    {
        v[i] = i;
    }
    josephus(m, n, k)
}