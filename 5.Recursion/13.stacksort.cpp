#include <bits/stdc++.h>
using namesapce std;

void StackSort(stack<int> &s)
{
    // using multimap method
    multimap<int> m;
    while (!s.empty())
    {
    }
}

int main()
{
    int n;
    stack<int> s;
    for (int i = 0; i < n; i++)
    {
        int x;
        cin >> x;
        s.push(x);
    }
}