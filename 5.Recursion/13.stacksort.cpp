#include <bits/stdc++.h>
using namesapce std;
#include <set>

void StackSort(stack<int> &s)
{
    // using multimap method
    multiset<int> m;
    while (!s.empty())
    {
        m.insert
    }
}

int main()
{
    int n;
    stack<int> s;
    for (int i = 0; i < n; i++)
    {
        int x;
        cin >> x;
        s.push(x);
    }
}