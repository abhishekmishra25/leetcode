#include <bits/stdc++.h>

using namespace std;
void generate_graph(vector<int> graph[], int u, int v)
{
    graph[u].push_back(v);
}

int main()
{
    vector<int> graph[4];
    for (int i = 0; i < 4; i++)
    {
    }
}