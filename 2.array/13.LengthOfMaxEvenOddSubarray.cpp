#include <bits/stdc++h>
using namespace std;

// create a function which tell max length of
// even odd occurring subaaray

int MaxlenSubArray(int arr[], int n)
{
    int count = 0, rsult = 0;
    for (int i = 0; i < n; i++)
    {
        if (i % 2 == 0 and arr[i] % 2 == 0)
    }
}

int main()
{
    int arr[] = {10, 12, 14, 7, 9};
    int n = 5;
}